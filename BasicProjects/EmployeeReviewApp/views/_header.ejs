<nav class="navbar navbar-expand-lg shadow" style="background-color: rgb(36, 7, 200)">
    <div class="container-fluid">
      <!-- Navbar brand -->
      <a class="navbar-brand fw-bold" href="/">
       
        Employee Review
      </a>
      <!-- Navbar toggler -->
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- Navbar collapse section -->
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <!-- Left-aligned navbar links -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0 fw-semibold">
          <% if(locals.user) { %> <% if( user.role.toString() == 'Admin' ) { %>
          <!-- Admin specific links -->
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/dashboard/admin">
              <i class="fa-solid fa-house"></i>
              Home
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/dashboard/admin/addEmployee">
              <i class="fa-solid fa-user-plus fa-beat"></i>
              Add Employee
            </a>
          </li>
          <% } else { %>
          <!-- Employee specific links -->
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/dashboard/employee">
              <i class="fa-solid fa-house fa-beat"></i></i>
              Home
            </a>
          </li>
          <% } %> <% } %>
        </ul>
        <!-- Right-aligned navbar links -->
        <div class="navbar-nav fw-semibold">
          <% if(locals.user) { %>
          <!-- Display user role -->
          <a class="nav-link active" aria-current="page" href="#">
            <i class="fa-solid fa-user fa-beat"></i>
            <%= user.role %>
          </a>    
          <!-- Logout link -->
          <a class="nav-link" href="/signout">
            <i class="fa-solid fa-right-from-bracket fa-beat"></i>
            Logout
          </a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
  